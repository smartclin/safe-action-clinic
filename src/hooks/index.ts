// Re-export useSession for direct access if needed
export { useSession } from '@/components/providers';

export { type UseAuthReturn, useAuth } from './use-auth';
export { useRequireAuth } from './use-require-auth';
